'use strict';

var types = require('../../../config/IOTypes'),
    Attacher = require('../Common').attach;

function ImagesHandler (io, socket) {
    var attach = Attacher(socket);



}

module.exports = ImagesHandler;